name: stock_forecast

conda_env: config/mlflow_env.yaml
# Can have a docker_env instead of a conda_env, e.g.
# docker_env:
#    image:  mlflow-docker-example

entry_points:
  main:
    parameters:
      kernel: {type: string, default: 'rbf'}
      C: {type: float, default: 100}
      gamma: {type: string, default: 'scale'}
      epsilon: {type: float, default: .1}
    command: "python stock_forecast.py {kernel} {C} {gamma} {epsilon}"
#      data_file: path
#      regularization: {type: float, default: 0.1}
#    command: "python main.py -r {regularization} {data_file}"

  validate:
    parameters:
      data_file: path
    command: "python validate.py {data_file}"